// import Vehicle from "../models/vehicleModel";

// export default async (id) => {
//   let vehicle;
//   vehicle = await Vehicle.findOne({ _id: id, isDeleted: false }).lean();
//   return vehicle;
// };
